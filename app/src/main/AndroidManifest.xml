<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shash.socio_sport">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity1"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sports" />
        <activity android:name=".football" />
        <activity android:name=".badminton" />
        <activity android:name=".Games" />
        <activity android:name=".account" />
        <activity android:name=".pubg" />
        <activity android:name=".csgo" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserDetailsPubgSession3" />
        <activity android:name=".UserDetailsPubGSession1" />
        <activity android:name=".UserDetailsPubGSession2" />
        <activity android:name=".UserDetailsPubGSession3" />
        <activity android:name=".UserDetailsPubGSession4" />
        <activity android:name=".UserDetailsPubGSession5" />
        <activity android:name=".UserDetailsCSGOSession1" />
        <activity android:name=".UserDetailsCSGOSession2" />
        <activity android:name=".UserDetailsCSGOSession3" />
        <activity android:name=".UserDetailsCSGOSession4" />
        <activity android:name=".UserDetailsCSGOSession5" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>